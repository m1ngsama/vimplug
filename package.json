{
  "name": "vimplug",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26"
  },
  "scripts": {
    "test": "node --test 'src/**/*.test.ts'",
    "test:e2e": "pnpm build:chrome && playwright test",
    "check": "tsc --noEmit",
    "icons": "node scripts/icons.ts",
    "build:chrome": "TARGET=chrome vite build && TARGET=chrome node scripts/manifest.ts && node scripts/check-size.ts chrome",
    "build:safari": "TARGET=safari vite build && TARGET=safari node scripts/manifest.ts && node scripts/check-size.ts safari",
    "build": "pnpm build:chrome && pnpm build:safari"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
